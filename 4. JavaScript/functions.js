// function dec.

printSum(5, 9);

function printSum(a, b) {
  console.log("sum is " + (a + b));
}

// function exp.

const printDiff = function (x, y) {
  console.log("Diff is " + (y - x));
};
printDiff(3, 19);

// arrow functions

const printPro = (a, b) => {
  console.log("product is " + a * b);
};
printPro(5, 6);
